import { Clausula } from "src/modelo/clausula.entity";
import { Recursos } from "src/modelo/recursos.entity";

export interface IClausulaService{

    // findAll(): Promise<Clausula[]>;
    // findById(id: number): Promise<Clausula>;
    // create(clausula: Partial<Clausula>,recurso: Recursos): Promise<Clausula>;
    // update(id:number, clausula: Partial<Clausula>, recursoId: number): Promise<Clausula>;
    // delete(id:number): Promise<void>;
}